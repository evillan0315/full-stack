export interface User {
  id: string;
  name: string;
  email: string;
  role: string;
  image?: string;
  phone_number?: string;
  emailVerified?: string;
  updateAt?: string;
  createdAt?: string;
}
